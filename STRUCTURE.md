# 手相鑑定アプリ — 構造ガイド
> エンジニア志望の初心者向け。専門用語は最小限にしています。

---

## 1. まず「Webアプリってどう動くの？」

スマホでこのアプリを使うとき、裏側でこんなことが起きています。

```
あなたのiPhone                       Renderのサーバー（クラウド上のPC）
─────────────────                    ────────────────────────────
│ Safari でURLを開く │ ──要求──→ │ ファイルを返す               │
│ 画面が表示される   │ ←─返答─── │                              │
│                   │            │                              │
│ 写真を撮って送信   │ ──要求──→ │ Claude AIに分析を依頼        │
│ 鑑定結果が出る     │ ←─返答─── │ 結果を返す                   │
─────────────────                    ────────────────────────────
        ↑                                       ↑
   「フロントエンド」                       「バックエンド」
 ユーザーが目で見る部分               裏側で処理する部分
```

**フロントエンド** = 画面・デザイン・ボタンなど見える部分  
**バックエンド** = データの処理・AIへの通信など見えない部分

---

## 2. ファイル構造の全体図

```
tesou/
│
├── server.js                ← バックエンドの入口。司令塔。
│
├── package.json             ← 「このアプリに必要な道具リスト」
│
├── lib/                     ← 特殊な処理のライブラリフォルダ
│   ├── analyzer.js          ← Claude AIへの通信を担当
│   └── preprocessor.js      ← 画像を分析しやすく加工する
│
├── data/
│   └── palmistry-db.js      ← 手相の知識データベース
│
└── public/                  ← フロントエンドのフォルダ
    ├── index.html           ← 画面の骨格（HTML）
    ├── style.css            ← 見た目・デザイン（CSS）
    └── app.js               ← 画面の動き・操作（JavaScript）
```

---

## 3. 各ファイルの役割（一言説明）

### 📋 `package.json` — アプリの設計書

```json
{
  "dependencies": {
    "express":       "Webサーバーを作るための道具",
    "multer":        "画像ファイルを受け取るための道具",
    "sharp":         "画像を加工するための道具",
    "@anthropic-ai/sdk": "Claude AIと話すための道具"
  }
}
```

**たとえ**: 料理に必要な食材リスト。`npm install` で全部まとめて買ってくる。

---

### 🚪 `server.js` — バックエンドの司令塔（70行）

```
役割: スマホからの「リクエスト」を受け取り、適切な処理に振り分ける

できること:
  GET /          → index.html を返す（画面を表示）
  POST /analyze  → 写真を受け取り、AIに分析させ、結果を返す
  POST /analyze-comparison → 両手の写真を受け取り、比較分析する
```

**たとえ**: レストランのフロアスタッフ。お客さんの注文を受けてキッチンに伝え、料理ができたらお客さんに届ける。

---

### 🤖 `lib/analyzer.js` — AI分析エンジン（約550行）

このアプリの一番重要なファイル。Claude Vision APIと3段階で会話する。

```
写真が届く
    ↓
【Stage 1: 観察】(Haiku モデル / 安価・高速)
  「この写真の手のひらを客観的に観察して。
   見えた特徴をJSON形式で報告して。
   見えないものは not_visible と書いて。」
    ↓ (JSONデータが返ってくる)

【画質チェック】
  手のひらが写っている? → OK
  写っていない or 暗すぎ → エラーを返して終了
    ↓

【Stage 2: 解釈】(Sonnet モデル / 高精度)
  「Stage1の観察結果と手相データベースを参照して、
   日本語で鑑定結果をJSON形式で生成して。」
    ↓ (鑑定結果JSONが返ってくる)

【Stage 3: 検証】(Sonnet / 高品質画像はスキップ)
  「この鑑定結果に矛盾はない?
   自信のない部分を断定的に書いていない?
   修正してJSON形式で返して。」
    ↓

完成した鑑定結果を server.js に返す
```

**なぜ3段階に分けるの?**  
一発で「解釈まで」やらせると、AIが見えない線を「あるはず」と思い込んで嘘をつきやすい（ハルシネーション）。事実の観察 → 解釈 → 検証と分けることで精度が上がる。

---

### 🖼️ `lib/preprocessor.js` — 画像前処理（30行）

```
やること:
  1. コントラスト正規化 → 薄い手相の線を浮き彫りにする
  2. 軽いシャープニング → 線のエッジをくっきりさせる
  3. JPEG形式に統一 → Claude APIに送りやすくする

失敗しても: 元の画像をそのまま使う（エラーにならない設計）
```

**たとえ**: 写真をAIに渡す前に「フィルター加工」する処理。

---

### 📚 `data/palmistry-db.js` — 手相データベース（約1150行）

Claude AIに渡す「教科書」。全体の約40%を占める最大ファイル。

```
中身の構造:
  handShapes  → 手の形（地・火・水・風）の意味
  lifeLine    → 生命線の長さ・深さ・軌跡ごとの解釈
  heartLine   → 感情線の意味
  headLine    → 知能線の意味
  fateLine    → 運命線の意味
  sunLine     → 太陽線の意味
  mounts      → 各丘（盛り上がり）の意味
  specialMarks → ます掛け・仏眼・神秘十字線などの意味
  combinations → 複数の線が組み合わさったときの特別な意味
  modernContext → 2026年の視点からの解釈

buildDatabaseContext() → 上記を凝縮したテキストを生成する関数
                         (これをClaudeのsystemプロンプトに渡す)
```

**たとえ**: 占い師が持っている分厚い専門書。AIはこれを参照しながら鑑定する。

---

### 🏗️ `public/index.html` — 画面の骨格（約400行）

```
画面の区画（セクション）:
  #uploadSection     → 最初の画面。カメラ/ギャラリーのボタン
  #previewSection    → 写真確認画面。手の選択・テーマ選択
  #loadingSection    → 「分析中...」のアニメーション画面
  #resultSection     → 鑑定結果の表示画面
  #compResultSection → 両手比較の結果画面
  #errorSection      → エラー時の案内画面
```

一度に表示するのは1つのセクションだけ。他は `hidden` クラスで非表示。

---

### 🎨 `public/style.css` — デザイン（約580行）

```
主な役割:
  - 色・フォント・余白の定義
  - ダークな星空っぽい背景アニメーション
  - カードUI・ボタン・バッジのスタイル
  - モードタブ・テーマボタンのスタイル
  - スマホ画面に最適化（レスポンシブ対応）

:root { } → アプリ全体で使う色の変数を定義
  --purple: メインカラー
  --gold:   アクセントカラー
  --bg:     背景色
```

**たとえ**: 建物の外観・インテリアデザイン。骨格（HTML）に服を着せる。

---

### ⚙️ `public/app.js` — 画面の動き（約620行）

```
主な役割:
  状態管理 → 「今どの画面?」「どの手が選択された?」を記録

  イベント処理:
    カメラ/ギャラリー → 写真を選んだら次の画面へ
    手の選択ボタン   → right/left/unclearを記録
    テーマボタン     → overall/love/career/healthを記録
    モードタブ       → single/comparisonを切替
    鑑定ボタン       → サーバーに写真を送信

  通信処理:
    画像を圧縮 → FormDataにまとめる → /analyze に送信 → 結果を受け取る
    (両手比較なら /analyze-comparison に送信)

  表示処理:
    受け取ったJSONデータを読んで
    各HTML要素（#openingText, #fortune2026 など）に文字を入れる
```

**たとえ**: 建物の中の電気・水道・エレベーターなど、見えないけど動かすための仕組み。

---

## 4. 写真を撮ってから結果が出るまでの流れ

```
① ユーザーが写真を撮る
   └─ app.js: handleFile() がファイルを受け取る

② プレビュー画面が表示される
   └─ app.js: showSection('previewSection')

③ 「鑑定する」ボタンを押す
   └─ app.js: compressImage() で1800px以下に圧縮
   └─ app.js: FormData に画像・手の選択・テーマをまとめる
   └─ app.js: fetch('/analyze', ...) でサーバーに送信

④ サーバーが受け取る
   └─ server.js: POST /analyze のハンドラが動く
   └─ preprocessor.js: コントラスト強調・シャープニング

⑤ AI分析（一番時間がかかる）
   └─ analyzer.js: Stage 1 (Haiku) → 観察JSON
   └─ analyzer.js: 画質チェック
   └─ analyzer.js: Stage 2 (Sonnet) → 鑑定JSON（DB参照）
   └─ analyzer.js: Stage 3 (Sonnet) → 検証・修正JSON ※高品質ならスキップ

⑥ 結果をスマホに返す
   └─ server.js: res.json(result) で返送

⑦ 画面に表示する
   └─ app.js: renderResult(data) が各テキストを画面に書き込む
   └─ app.js: showSection('resultSection')
```

---

## 5. 重要な技術用語をざっくり説明

| 用語 | 意味 | このアプリでは |
|------|------|----------------|
| **HTML** | 画面の骨格を作る言語 | index.html |
| **CSS** | 見た目を装飾する言語 | style.css |
| **JavaScript** | 動きをつける言語 | app.js (フロント) / server.js (バック) |
| **Node.js** | JavaScriptをサーバーで動かす仕組み | バックエンド全体 |
| **Express** | Webサーバーを簡単に作れるツール | server.js |
| **API** | 外部サービスと会話する窓口 | Claude Vision API |
| **JSON** | データを整理して渡す形式 | AIとのやりとり全体 |
| **npm** | JavaScriptのパッケージ管理ツール | `npm install` でライブラリ導入 |
| **非同期処理 (async/await)** | 時間のかかる処理を待つ仕組み | API呼び出し全体 |
| **プロンプト** | AIへの指示文 | OBSERVATION_PROMPT など |

---

## 6. 設計上の工夫

### プロンプトキャッシュ
```
毎回同じDBデータをAPIに送るのは無駄 → cache_control をつけることで
Anthropicのサーバーがキャッシュして2回目以降は爆速・低コストになる
```

### Object URLの解放
```
写真を画面に表示するとき、ブラウザのメモリにURLが作られる
使い終わったら URL.revokeObjectURL() で解放しないとメモリリークになる
→ app.js の currentPreviewUrl で管理
```

### エラーの多層防御
```
画像が届かない → server.js でチェック
手が写っていない → analyzer.js でチェック
JSONが壊れている → parseJSON() でチェック
APIが遅い → withTimeout() でタイムアウト
サーバー落ちる → Stage3失敗してもStage2結果で継続
```

### なぜJSONでやりとりするの?
```
AIの返答が「文章」だと、プログラムで読みにくい
「感情線は長く...」という文章は機械には扱いにくい

JSONなら構造化されていてプログラムで読みやすい:
{
  "heart": {
    "reading": "感情線は長く...",
    "confidence": "high"
  }
}
→ data.interpretation.lines.heart.reading で直接アクセスできる
```

---

## 7. エンジニア学習のためのロードマップ（おすすめ順）

このアプリを理解するために必要な知識の順番:

```
1. HTML/CSS の基礎          → 画面を作る最初の一歩
2. JavaScript の基礎        → 動きをつける
3. Node.js / npm の基礎     → サーバーを動かす
4. HTTP / REST API の概念   → サーバーとの通信の仕組み
5. 非同期処理 (Promise/async) → 待つことの仕組み
6. JSON の読み書き          → データのやりとり
7. Claude / LLM API の使い方 → AIとの対話
```

このアプリはこれら**全部**を使っています。
逆に言うと、このコードを全部読み解けるようになったら、
Webアプリ開発の基礎がほぼ揃った状態です。
